# Arm GPS — map packages

Region packages (vector tiles + offline search index) for the Arm GPS Android app.

- `manifest.json` — catalog of available regions; the app fetches it from the raw URL of the `main` branch.
- Binary packages are attached to releases, one release per region version.

Data © OpenStreetMap contributors, ODbL. Tiles built with Planetiler (OpenMapTiles schema).
